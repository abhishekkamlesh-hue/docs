---
title: "Get All Contacts"
api: "GET /fetchContacts"
description: "Retrieve a paginated list of all contacts."
---

## Headers

<ParamField header="Cookie" type="string" required>
  Authentication token from login (httpOnly cookie)
  
  Example: `token=your_jwt_token_here`
</ParamField>

<ParamField header="Accept" type="string" default="application/json, text/plain, */*">
  application/json, text/plain, */*
</ParamField>

<ParamField header="Origin" type="string">
  Origin of the request (e.g., https://vb-seamless-dev-backend.oriserve.com/)
</ParamField>

<ParamField header="User-Agent" type="string">
  Browser or client user agent
</ParamField>

## Query Parameters

<ParamField query="limit" type="integer" default="50">
  Maximum number of contacts to return
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Number of contacts to skip for pagination
</ParamField>

<RequestExample>

```bash cURL
curl --request GET \
  --url 'https://vb-seamless-dev-backend.oriserve.com/fetchContacts?limit=50&offset=0'
```

</RequestExample>

<ResponseExample>

```json 200 Success
{
  "success": true,
  "data": {
    "contacts": [
      {
        "id": "contact_123",
        "name": "John Doe",
        "phone_number": "+919876543210",
        "email": "john@example.com"
      }
    ],
    "pagination": {
      "total": 5000,
      "limit": 50,
      "offset": 0
    }
  }
}
```

</ResponseExample>
